<%- include('../partials/head', {tituloWeb: 'Antiria - Detalle Producto'}) %>

<body>

    <!-- HEADER -->
    <%- include("../partials/header")%>
    <!-- FIN HEADER -->

    <main class="main-detalle-prod">
        <div class="main-contenedor-detalle-prod">


            <div class="contenedor-producto">
                <div class="detalle-prod-main">
                    <div class="imagenes-web">
                        <div class="imagen-del-producto">
                            <img class="foto-producto" src="/img/products/<%= prodUD.image %>" alt="foto-de-producto">
                        </div>
                        <div class="carousel">
                            <div class="item"><img src="/img/products/<%= prodUD.image %>"></div>
                            <div class="item"><img src="/img/products/<%= prodUD.image %>"></div>
                            <div class="item"><img src="/img/products/<%= prodUD.image %>"></div>
                            <div class="item"><img src="/img/products/<%= prodUD.image %>"></div>

                        </div>
                    </div>
                    <div class="nombre-precio">

                        <h3>
                            <%= prodUD.nombreProducto %>
                        </h3>

                        <h4>
                            $<%= prodUD.precioProducto %>
                        </h4>
                        <% if (locals.isLogged) { %>
                            <form action="/productDetail/<%= prodUD.id %>/<%= locals.userLogged.id %>" method="POST">
                                <button class="submitCart" type="submit">Agregar al carrito</button>
                            </form>
                        <% } else { %>
                            <form action="/login">
                                <button class="submitCart" type="submit">Agregar al carrito</button>
                            </form>
                        <% } %>

                    </div>
                </div>
            </div>
            <div class="descripcion-prod">
                <h4>Descripción del producto</h4>
                <hr/>

                <h4>
                    <%= prodUD.nombreProducto %>
                </h4>

                <h5>
                    <%= prodUD.descripcionGeneral %>
                </h5>

            </div>
        </div>


    </main>

        <!-- FOOTER -->
        <%- include("../partials/footer")%>
            <!-- FIN FOOTER -->

</body>

</html>